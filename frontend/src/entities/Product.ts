interface Product {
  categories: number[];
  name: string;
  slug: string;
  description: string;
  unit_price: number;
  discount: number;
  price: number;
  stock: number;
}

export default Product;
